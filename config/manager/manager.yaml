apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: agentic-autoscaler
    app.kubernetes.io/managed-by: kustomize
  name: system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: agentic-autoscaler
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    matchLabels:
      control-plane: controller-manager
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: controller-manager
    spec:
      # TODO(user): Uncomment the following code to configure the nodeAffinity expression
      # according to the platforms which are supported by your solution.
      # It is considered best practice to support multiple architectures. You can
      # build your manager image using the makefile target docker-buildx.
      # affinity:
      #   nodeAffinity:
      #     requiredDuringSchedulingIgnoredDuringExecution:
      #       nodeSelectorTerms:
      #         - matchExpressions:
      #           - key: kubernetes.io/arch
      #             operator: In
      #             values:
      #               - amd64
      #               - arm64
      #               - ppc64le
      #               - s390x
      #           - key: kubernetes.io/os
      #             operator: In
      #             values:
      #               - linux
      securityContext:
        runAsNonRoot: true
        # TODO(user): For common cases that do not require escalating privileges
        # it is recommended to ensure that all your Pods/Containers are restrictive.
        # More info: https://kubernetes.io/docs/concepts/security/pod-security-standards/#restricted
        # Please uncomment the following code if your project does NOT have to work on old Kubernetes
        # versions < 1.19 or on vendors versions which do NOT support this field by default (i.e. Openshift < 4.11 ).
        # seccompProfile:
        #   type: RuntimeDefault
      containers:
      - command:
        - /manager
        args:
          - --leader-elect
          - --health-probe-bind-address=:8081
        image: controller:latest
        # IfNotPresent so kind/dev clusters use the locally-loaded image
        # rather than trying to pull `:latest` from a registry that
        # doesn't exist. In production, override IMG to a registry-tagged
        # immutable digest and policy becomes a non-issue.
        imagePullPolicy: IfNotPresent
        name: manager
        env:
          # Required (controller panics on startup if either is unset).
          # See docs/design_v2.md §4 for the full env-var inventory.
          #
          # The Service exposes port 80 (targeting the container's 8000);
          # see deploy/manifests/forecast-service.yaml. We omit the port
          # here so port 80 is implied — the previous explicit ":8000"
          # made every forecast call connection-refused, the controller
          # surfaced ForecastUnavailable Events, and predicted_rps was
          # never populated.
          - name: FORECAST_SERVICE_URL
            value: "http://forecast-service.agentic-system.svc"
          - name: PROMETHEUS_URL
            value: "http://kube-prom-kube-prometheus-prometheus.monitoring.svc:9090"
          # Optional — Ollama lives outside the cluster by default, so
          # in-cluster installs leave OLLAMA_URL unset (worker no-ops on
          # connection refused, see internal/explainer/worker.go).
          - name: OLLAMA_MODEL
            value: "llama3.2"
          # =====================================================
          # HACKATHON OVERRIDES (hackathon branch only)
          # =====================================================
          # All values below override code defaults in internal/config/
          # config.go for the June 2 hackathon submission. Goal: make
          # the controller engage faster on short k6 runs (10-25 min)
          # so the "predicted RPS approximately equal to actual RPS"
          # demo claim is visible within the scenario window.
          # Restore production defaults by deleting this block.
          #
          # Tier 1: hot-path responsiveness
          # 10 -> 3: first forecast call after 3 min instead of 10
          - name: HOT_PATH_MIN_POINTS
            value: "3"
          # 60 (code default) -> 60 (kept), bumped back from the earlier
          # 15-min hackathon value. The 15-min window was tuned for the
          # bursty/spiky scenarios where post-run zeros poisoned the
          # forecaster; the diurnal + 24h soak runs need the full 60-min
          # window so Prophet's slope estimate has enough context. With
          # the new persistent Prometheus (PVC + 30h retention), the
          # zero-tail problem the 15-min value was working around no
          # longer happens — Prometheus survives between runs.
          - name: HOT_PATH_HISTORY_MINUTES
            value: "60"
          # 60 -> 30: 2x more reconcile cycles per minute. Trades a
          # small bump in Prometheus + forecast-service load for much
          # finer-grained scaling decisions.
          - name: RECONCILE_INTERVAL_SECONDS
            value: "30"
          # Tier 2: classifier engages within the demo window
          # 72 -> 22 (the validation floor at default resolution=5).
          # First classification arrives at ~22 min instead of ~72.
          - name: CLASSIFIER_MIN_POINTS
            value: "22"
          # 30 -> 5: classifier worker re-runs 6x more often, so a
          # 20-min scenario sees 4 classifications instead of zero.
          - name: CLASSIFIER_INTERVAL_MINUTES
            value: "5"
          # 24 -> 25: cold-path PromQL query window. This is NOT a delay
          # before the classifier runs — the worker still fires every
          # CLASSIFIER_INTERVAL_MINUTES regardless. This sets how much
          # history each invocation queries.
          #
          # The previous hackathon value of 1h was a hard bug: at default
          # CONTEXT_DOWNSAMPLE_RESOLUTION_MIN=5 the query returns at most
          # 60/5=12 samples, but CLASSIFIER_MIN_POINTS=22 requires >=22.
          # 12 < 22 means the classifier logged "insufficient data" on
          # every invocation and NEVER produced output. The "Classified
          # Pattern" panel sat at 0/Default forever.
          #
          # 25h gives the diurnal scenario room to see a full 24h cycle
          # plus 1h slack. Once the cluster has been up >110 min
          # (22 points x 5-min resolution), the classifier engages; at
          # >12h elapsed Prophet's hourly_profile regressor also turns
          # on; at >24h elapsed the classifier finally sees a full
          # diurnal shape and can stamp PatternPeriodic.
          - name: CLASSIFIER_HISTORY_HOURS
            value: "25"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - "ALL"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        # TODO(user): Configure the resources accordingly based on the project requirements.
        # More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      serviceAccountName: controller-manager
      terminationGracePeriodSeconds: 10
